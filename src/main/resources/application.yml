server:
  port: 8088

spring:
  application:
    name: ms-seguridad
  datasource:
    url: jdbc:mysql://localhost:3306/db_seguridad?createDatabaseIfNotExist=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/